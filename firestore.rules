rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // ⚠️ DEVELOPMENT ONLY — open rules for testing
    // TODO: Replace with production rules before client handover
    match /{document=**} {
      allow read, write: if true;
    }

  }
}
